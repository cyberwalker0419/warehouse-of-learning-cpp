#include<bits/stdc++.h>
int n;
void dfs(int now){
    if(1){
        //计数一类的
        return;
    }
    for(int i=1;i<=n;i++){
        if(1){
            //mark(标记)
            dfs(now+1);
            //undo(回滚)
        }
    }
}