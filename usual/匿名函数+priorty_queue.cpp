#include <bits/stdc++.h>
typedef long long ll;
#define N 100005
#define INF 0x7fffffff
#define INFLL 0x7fffffffffffffff
struct node{
    int x,y;
    bool operator<(const node& rhs)const{
        return x<rhs.x;
    }
};
int main(){
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    std::cout.tie(0);
    auto check=[&](int x){

    };
    std::function<void(int)> name=[&](int a){

    };
    int n;
    std::vector<node> a(n);
    std::set<node> s;
    return 0;
}
